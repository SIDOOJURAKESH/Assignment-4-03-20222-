#include<stdio.h>
//this is called recursion and this would cause the stack overflow.
int main(void)//recursion in main
{
main();
return 0;
}
//it not a wroing code but it is batcode the difference is it is batcode
